[build]
  # Only rebuild when admin-site folder changes (saves build minutes)
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF admin-site/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
